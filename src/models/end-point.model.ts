// enum Auth {
//   signIn = "/token",
//   signUp = "/register",
// }

enum Car {
  base = "/cars/",
}

// enum Venue {
//   base = "/venue/category/",
// }

export const EndPoints = {
  //auth: Auth,
  car: Car,
  //venue: Venue,
};
